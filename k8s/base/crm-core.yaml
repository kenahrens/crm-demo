apiVersion: v1
kind: ConfigMap
metadata:
  name: crm-core-config
data:
  DB_HOST: "postgres"
  DB_PORT: "5432"
  DB_USER: "postgres"
  DB_NAME: "crm"
  DB_SSLMODE: "disable"
  PORT: "8080"
  GIN_MODE: "debug"
---
apiVersion: v1
kind: Secret
metadata:
  name: crm-core-secrets
type: Opaque
data:
  DB_PASSWORD: cG9zdGdyZXM= # postgres (base64 encoded)
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: crm-core
  labels:
    app: crm-demo
    component: crm-core
spec:
  replicas: 1
  selector:
    matchLabels:
      app: crm-demo
      component: crm-core
  template:
    metadata:
      labels:
        app: crm-demo
        component: crm-core
    spec:
      containers:
      - name: crm-core
        image: ghcr.io/kenahrens/crm-demo/crm-core:v1.3.8
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: crm-core-config
        - secretRef:
            name: crm-core-secrets
        readinessProbe:
          httpGet:
            path: /v1/api/health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /v1/api/health
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 20
        resources:
          limits:
            cpu: "500m"
            memory: "512Mi"
          requests:
            cpu: "100m"
            memory: "128Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: crm-core
  labels:
    app: crm-demo
    component: crm-core
spec:
  selector:
    app: crm-demo
    component: crm-core
  ports:
  - port: 80
    targetPort: 8080
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: crm-core-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  rules:
  - http:
      paths:
      - path: /api(/|$)(.*)
        pathType: Prefix
        backend:
          service:
            name: crm-core
            port:
              number: 80

